# daily-assistant 优化清单

> 项目: daily-assistant — OpenCode 日常陪伴助手（把编程 agent 改造成陪伴/管家/复盘助手，最终并入安卓 App）
> 交付物: `plugin/`（可直接装入 OpenCode） | 沙箱: `workspace/`
> 基线: OpenCode 1.18.29（Termux 移植版） | 测试: 隔离环境跑 `opencode run --agent <role> "<话>"`
> 插件源码: `plugin/plugin/daily-companion.ts` | 角色: `plugin/agent/*.md`

## 文档规范

1. **本文件只跟踪未完成项与在办项**，已闭合的迭代写一行结论即可
2. **迭代编号** — DC-N，完成即闭合，新迭代追加到末尾
3. **状态标记** — ⏳ 待定 | 🔄 进行中 | ✅ 完成 | ❌ 阻塞/暂缓
4. **隔离铁律** — 任何实验必须同时隔离 `XDG_CONFIG_HOME` + `XDG_DATA_HOME`，禁止污染主库（见 DC-7）

---

## 当前状态：DC-1~DC-7、DC-13~DC-15、DC-20 ✅ 已完成 ｜ DC-16 🔄 进行中 ｜ DC-8~DC-12、DC-17~DC-19 ⏳ 未开始

- **版本**：插件 v0.1（后端）；未打包、未并入 App。
- **已验证能力**：记忆增/改/删、字数上限、去重、敏感扫描、长期待批准、历史检索（仅陪伴会话）、梦境复盘+日记，均实测通过。

---

## DC-1 ✅ 插件骨架 + 三角色（2026-09-13）

- 交付 `plugin/agent/` 下三个角色：`companion`（默认，陪伴）、`monitor`（手机管家）、`scheduler`（定时唤醒）。
- `plugin/opencode.jsonc`：默认角色、模型、技能路径、记忆注入。
- 验证：`opencode agent list` 三角色齐；persona 生效（读到 USER.md 的"讨厌啰嗦"）。

## DC-2 ✅ 记忆系统：增删改 + 上限 + 去重 + 敏感扫描（2026-09-13）

- `memory` 工具：`add/replace/remove`，`replace/remove` 用子串定位（多匹配报错）。
- 长期档案上限：USER 1375 字 / MEMORY 2200 字；**写满报错并回显当前条目，逼 AI 当场整理**。
- 写入前自动去重（保留首个）+ 敏感内容/注入扫描。
- 原子写（临时文件 + rename）防写坏。
- 验证：记住偏好 ✓ / 修改旧条目 ✓ / 灌满后新增触发报错→自行删旧腾位→写入 ✓ / 跨会话回忆 ✓。
- ⚠️ **2026-09-15 弃用**：整套自研记忆（含下列 DC-3/DC-4）由 DC-20 记忆引擎取代，插件内相关工具与文件已拆除。

## DC-3 ✅ 长期写入待批准队列（2026-09-13）

- 用户拍板：`target=daily` 立即写；`target=user/memory` 进 `pending.md` 待批准。
- `memory_review`：`list` / `approve`（all 或指定 id）/ `reject`。
- 验证：提案不入档且提示"待确认" ✓ / 批准后写入 USER.md 且队列清空 ✓。
- ⚠️ **2026-09-15 弃用**：新引擎改为"自动记录、模型把关"，待批准队列取消（DC-20）。

## DC-4 ✅ 历史检索：只搜手机端陪伴会话（2026-09-13）

- `recall_history` 直读 OpenCode 会话库（`opencode.db`），按 `session.agent IN (companion/monitor/scheduler/dream)` 过滤。
- **范围铁律**：只搜手机端日常助手，绝不触碰电脑端与编程会话（用户要求）。
- 库路径可用 `DAILY_COMPANION_SESSION_DB` 覆盖。
- 验证：搜索"咖啡"命中历史原话 ✓。
- ⚠️ **2026-09-15 弃用**：改为引擎的 `conversation/search`（在引擎内部按身份隔离，范围更严）（DC-20）。

## DC-5 ✅ 深夜复盘 dream 角色 + 梦境日记（2026-09-13）

- `dream` 子角色：读最近流水 → 提炼长期候选 → 提交待批准 → 写 `dreams.md`。
- `dream_diary` 工具写第一人称日记；日记只给人看，**不作为记忆来源**（对齐 OpenClaw）。
- 验证：一次复盘提 2 条候选 + 写日记 ✓。

## DC-6 ✅ 手机能力（2026-09-13）

- `phone_status` 工具：电量/充电/电池温度/CPU 温度/内存/运行时长。
- `phone-hands` 技能：截图、读屏文字、点击/滑动/输入、启动 App、通知/说话/震动（依赖 Termux:API 与 adb 无线调试）。
- 验证：读出电量/温度/内存/54 天未重启，并主动提醒温度偏高 ✓。

## DC-7 ✅ 测试隔离规范 + 污染事故清理（2026-09-13）

- **事故**：早期测试只隔离配置、未隔离数据目录 → 测试会话写进主库，暴露到用户 App。
- **修复**：确立"双隔离"（`XDG_CONFIG_HOME` + `XDG_DATA_HOME`）；并删除主库中我造的 13 个测试会话（22 → 9，剩余均用户自有）。
- **规范**：此后所有实验一律双隔离，禁止在主库产生会话。

---

## DC-8 ⏳ 并入安卓 App（未开始）

- 目标：把 `plugin/` 整套并入 `OConnector-Pro`，随 App 开关。
- 待定：内嵌方式（打包 Bun+opencode 引擎 / 嵌终端+完整环境）——两条路线已验证可行性，待用户拍板。
- 依赖：用户 App 源码（GitHub `LJH233333/OConnector-Pro`，当前仓库未含 `app/src`）。

## DC-9 ⏳ 夜间定时触发（未接）

- 现状：`dream` 需外部定时器触发；手机上可用 Termux cron，App 内用 App 定时器。
- 待办：手机端先配一个夜间定时（几点待定），跑 `opencode run --agent dream "开始今晚的复盘"`。

## DC-10 ⏳ 晋升评分门槛（OpenClaw 式，未做）

- 现状：`dream` 提案无评分门槛，靠模型判断，可能提案偏多。
- 计划：引入"评分 + 被回忆次数 + 查询多样性"三道门槛后再提案（OpenClaw 默认 0.75/3/3）。

## DC-11 ⏳ 向量检索记忆（评估后暂不采用）

- 调研过 `opencode-mem`（本地向量库，1642★，MIT）等，能力强但依赖嵌入模型 + AI 接口，体积与成本不适合手机端嵌入。
- 结论：先用"小档案 + 全文检索"，需要时再评估。

## DC-12 ⏳ 手机能力原生化（未开始）

- 现状：`phone_status` 依赖 Termux:API；截图/操作依赖 adb。
- 计划：App 内改为原生接口（UsageStatsManager / 无障碍 / 通知监听），并做成标准工具，见手机能力设计（电量/用量/屏幕/操作/通知）。

---

## DC-13 ✅ 记忆模板与实盘分离 + 公开仓库（2026-09-13）

- **问题**：记忆文件以后会被写入个人数据，若与模板同文件，公开仓库会泄露隐私。
- **做法**：仓库只保存 `USER.template.md` / `MEMORY.template.md`；实盘 `USER.md`/`MEMORY.md`、`daily/`、`pending.md`、`dreams.md` 及凭据、`workspace/`、`*.db` 由 `.gitignore` 排除。
- 新增 `install.sh`：装到 OpenCode；实盘记忆不存在时才用模板生成（不覆盖）；`opencode.jsonc` 已存在则备份不覆盖；支持 `OPENCODE_CONFIG_DIR` 指到隔离目录。
- 建**公开**仓库并推送：https://github.com/LJH233333/daily-assistant
- 验证：云端文件清单 18 个，无实盘记忆/凭据/沙箱；工作树 + git 全历史扫描零真实密钥。

## DC-14 ✅ 项目规范沉淀（2026-09-13）

- `AGENTS.md` 重写：五条铁律（新增"公开仓库红线"）、命令速查、**脱敏与凭证专节**、8 条坑+反例。
- 与全局规范去重（§0 对应表）；消解"全局允许另起服务 vs 本项目不碰运行实例"的潜在冲突（项目收紧）。
- 新增坑：实盘记忆入公开仓库、对全局配置跑 install.sh、本环境 task 子进程可能被拒。

---

## 关联记录

- 云端仓库（公开）：https://github.com/LJH233333/daily-assistant
- 记忆哲学调研：Hermes Agent（MIT，`NousResearch/hermes-agent`）与 OpenClaw（`openclaw/openclaw`）对比，结论 = 两者合体（小档案常驻 + 历史可搜 + 夜间门槛化晋升）。
- ⚠️ 安全提示：OpenClaw 仓库指标异常（星标/issue/体积离谱）、安装脚本不在仓库内，**仅作设计参考，禁止运行其安装脚本**。
- 已暂缓路线：`workspace/termux-app`（fork Termux 嵌终端）与 GitHub 仓库 `LJH233333/termux-assistant`（Android 内嵌实验，已停）。

## DC-15 ✅ 网页测试前端（2026-09-13，完成）

- 单文件 `companion.html`：连本机服务聊天（新建对话/历史/快捷按钮：打招呼、查手机、复盘），走 `POST /session` + `prompt_async` + 轮询读消息。
- 链路已验证：隔离服务（4123 端口）建会话→发话→陪伴角色回话 ✓；JS 打包无语法错 ✓；待用户在手机浏览器里实点。
- 附带发现：正在跑的主服务（4099）里**没装**陪伴插件（只有编程角色），所以测试页默认连 4123 隔离服务；要让 App/主服务用上，得经用户同意后正式安装+重启。
- 测试服务重启命令：`T=~/.cache/opencode/tmp/da-web; XDG_CONFIG_HOME=$T/config XDG_DATA_HOME=$T/data nohup opencode serve --port 4123 --hostname 127.0.0.1 > $T/serve.log 2>&1 &`
- 实测修了两个卡点：①陪伴提示词里写死了旧记忆路径，模型跑去翻不存在的文件卡死，已改成只用 `recall` 工具；②聊天目录别指项目根（里面 2G 源码会让文件搜索转半天），页面已改用 `da-web/chat` 小目录，"你是谁"实测 40 秒内回话 ✓。
- 用户实点通过 ✓；4123 定为本项目**专用测试端口**（已写进 AGENTS.md 铁律一）。

## DC-16 🔄 长记忆封版方案（2026-09-13，用户已拍板，实施中）

- 实施顺序：①条目时间戳 ✅ → ②打分+待批准 ✅（2026-09-13 实测：5 分进队列→批准→USER.md+scores.json 侧车账本，主库零污染） → ③回忆录装订+索引 → ④快照预算+钉住 → ⑤自测题。
- 封版要点：单一预算默认 20000（核心 3000 钉死+扩展 17000）；过期不删只标过期；压缩/打分/装订静默；动家底必须用户批准；回忆录 L0/L1/L2 编号+dream 出 Mermaid 人生画布。
- 记忆管理小模型：**固定小米 MiMo V2.5**，不用本地模型（用户拍板）。
- 测试页已加钥匙栏：模型锁死 `mimo-v2.5`，钥匙只存本机浏览器、只发小米接口；"测试钥匙"按钮验的是管理员本人回话（身份：只管长记忆，不管陪聊）。
- 验收：50 道自测题，45+ 通过；分数记趋势。
- 2026-09-13 五题实测全过：A教B考（猫名/球日/陷阱题正确拒绝/当日计数）+跨会话翻旧账（找得回今天第一句话，没有的诚实说没有）。

## DC-17 ⏳ 烘焙：冻结源码改出日常助手版（用户已拍板，未开始）

- 基线：`workspace/opencode-src` 冻结在 95daf90（MIT），不再跟上游。
- 现状：android/arm64 目标已加；`agent.ts` 三 import 未注册；三 txt 是英文旧版（含旧路径 bug）。
- 方向修正（2026-09-13）：**单助手全功能**（只 `companion`，定时/复盘是触发方式不是独立角色）；测试端口 4123。
- 四步：封存打 tag → 接线+中文人设（含 dream 触发）→ 工具烘焙内置 → 编译+隔离实测封 v1。
- 切换 4099 运行实例需用户点头+挑时间（唯一打扰步骤）。

## DC-18 ⏳ 主动开口方案（已调研，未细化）

- 三档：提醒直达 / 关心先敲门 / 闲聊只被动；已读不回两次闭嘴一天；初期只单人场景；从用户反应里学时机（EOPA 思路）。

## DC-19 ⏳ 活人感：让助手不像机器（用户已提，待专门做）

- **要求**：助手必须像活人，不像查表机器。这是硬要求，不是锦上添花。
- **已落的初步规则**（写进 `plugin/agent/companion.md`）：
  - 记忆是背景不是台词：知道忌讳要说不出"你过敏"却能自然避开；禁用"根据你的记录/你之前说过"这类句式。
  - 知道得少 → 反问引导；知道得多 → 脱口而出给**一个**具体答案（如"晚上吃热干面吧"），不列菜单、不附营养分析。
  - 建议要避开近期重复（刚吃过面就别再推面）。
- **待做**（后续专门迭代）：
  - 把"活人感"落成可测的行为清单（正向/反例对照），纳入人设与验收。
  - 结合记忆层：区分"信息够/不够"的判据（画像+近期事实覆盖度）。
  - 语气、节奏、情绪回应的整体打磨；必要时抽 INTIMA 类考题验收（见研究报告 RQ1/RQ6）。
- 关联：记忆引擎只负责"记得住"，活人感靠人设+上下文组织，两者要一起调。

---

## DC-20 ✅ 记忆引擎融合（腾讯 TencentDB Agent Memory）（2026-09-15）

> 起源：用户要求把腾讯开源记忆系统并入本项目（DC-16 封版方案之外的新路线），
> 替代 DC-3/DC-4 那套自研记忆。**旧机制先并存、验证通过再拆**。

### 用户拍板的决策

1. **嵌入方式**：代理式 —— 助手请求绕腾讯 MemoryProxy 一圈，自动存 + 自动取。
2. **碎片记忆取法**：保持官方设计（L0/L1 不自动注入，给助手只读工具自己查；L2/L3 自动注入）。
3. **旧记忆机制**：弃用（新引擎全面接管），但先并存，验证通过再拆。
4. **管理面板**：手机装不了官方版（要 Docker），改为 `companion.html` 加"记忆库"可视化入口（已做）。
5. 记忆提炼模型：**小米 MiMo V2.5**（延续 DC-16 决定）；陪聊仍走 opencode-go。

### 已完成并实测（✅）

- 轻量版三服务在沙箱跑通：引擎（8420）+ 代理（8096）+ 小转发（8799，给 opencode-go 补会话头）。
- 引擎三层记忆（L0 原话 / L1 碎片 / L2 场景 / L3 画像）**全链路验证**：
  假聊天灌入 → 自动提炼（`[instruction]`/`[persona]`/`[episodic]`）→ L2 场景 → L3 画像。
- 真助手（companion）端到端跑通：经代理对话 → 身份识别正确 → 工具注入 + 画像注入 →
  **助手自己 curl 记忆桥查到"在减肥、吃得清淡"** → 回答"清蒸鱼 + 白灼青菜"（自然避开重口）→
  对话与工具调用**完整落库**（用户话 + recall + bash + 回复，身份四元组全对）→ 触发 L1/L2 提炼。
- `companion.html` 记忆库面板：4 页签（画像/场景/记忆/原话），5 个只读接口全通。

### 待完成

| # | 项 | 说明 |
|---|---|---|
| ① | ~~真助手接上代理并验证~~ | ✅ 2026-09-15 完成（见上） |
| ② | ~~三个服务统一启停脚本~~ | ✅ 2026-09-15 完成：`~/.cache/opencode/tmp/tdai/tdai-ctl.sh start\|stop\|restart\|status`（安卓上 lsof 看不到端口，改用健康接口 + pgrep） |
| ③ | 沙箱 2 处源码补丁未记录 | CORS 加 `x-tdai-service-id`、Node 版本检查放开到 `>=22`；升级腾讯代码会丢，需重打（已记入 `AGENTS.md` §3.6 与沙箱备忘） |
| ④ | 项目文档更新 | ✅ 2026-09-15：`优化清单.md`（DC-20）、`AGENTS.md`（§3.6 + 坑 13~16）、`HANDOFF.md` 均已补 |
| ⑤ | 引擎里假数据没清 | ✅ 2026-09-15 清过一次（身份保留，见沙箱备忘 §7 清库步骤）；⑥ 测试后需再清一次收尾 |
| ⑥ | ~~旧记忆机制没拆~~ | ✅ 2026-09-15 完成：插件只留 `phone_status`/`dream_diary`；人设、dream/scheduler 说明、README、安装脚本、配置、模板文件全部同步；隔离实测真助手改用引擎检索成功 |
| ⑦ | 开机自启没做 | ⏸ 用户 2026-09-15 拍板：**先不做** |
| ⑧ | 沙箱体积 1.2G | ⏸ 用户 2026-09-15 拍板：**先不动**（引擎运行必需：Core 792M + Proxy 353M） |

### 验收：用户实测通过（2026-09-15）

- 网页 `companion.html`（聊天 + 记忆库）用户实点通过；测试留下 36 条对话，引擎自动提炼出 4 条记忆
  （加班作息、饮食偏好、购物事件等），画像/场景随内容积累生长。
- 测试中发现并修好两处（都会表现为"面板是空的"）：
  1. 记忆库查询没带身份隔离字段 → 静默返回 0 条；已改为请求体带 team/agent/user 三件套。
  2. 浏览器跨域只放行固定的几个头，新增身份头会被拦；改为**身份走请求体**（不必改服务端白名单）。
- 网页入口：本机小服务 `http://127.0.0.1:4130/companion.html`
  （起法 `~/.cache/opencode/tmp/da-web/serve-companion.sh`；改完页面要重跑一次同步）。

### 遗留观察（下一个迭代处理）

- ~~L0 里助手消息带工具调用原始 JSON（噪声）~~ ✅ 2026-09-15 已修：
  沙箱**补丁③**（`MemoryProxy/src/handler.ts`，流式 L0 只写助手正文），实测已无噪声。
- L0 里同一条用户消息会**重复出现**（多轮工具调用的循环里，每轮都写一次）——小噪声，
  是否去重待评估（涉及代理录制逻辑，改前先想清楚会不会误删正常消息）。
- `tdai-gateway.yaml` 的 pipeline 节奏仍是快测值，正式长期用之前恢复默认。

### 配置融合（2026-09-15 完成）

- `plugin/opencode.jsonc`：注册 `tdai` provider（→ 本机代理 8096），apiKey 用
  `{file:~/.local/share/opencode/tdai-user-key}`（钥匙不落配置）。
- `plugin/agent/companion.md`：frontmatter 加 `model: tdai/deepseek-v4.1-flash`
  → **只有陪伴角色走代理**；全局默认模型不变（防代理没起时影响用户其他 OpenCode 使用）。
- 会话头无需配置：OpenCode 自带，代理能识别（实测注入生效、身份归属正确）。
- 隔离实测：改配置后真助手回话自然（跨会话记起"豆豆"），对话正常落库。

### 关键信息

- **架构**：助手 ↔ 代理 8096（存 L0 / 注入 L2+L3 / 暴露只读工具）↔ 引擎 8420；
  模型出口经小转发 8799 → opencode-go。
- **只读工具**（助手用 Bash+curl 调，代理自动注入身份）：`atomic/search`、`atomic/query`、
  `conversation/search`、`conversation/query`、`scenario/ls`、`scenario/read`，路径前缀 `/memory-bridge/v3/`。
- **自动注入前提**：请求必须带会话头（`x-session-id` / `x-conversation-id`），身份四元组
  （team/user/agent/session）缺一不可，否则静默跳过。
- **运维细节**（ID、起停命令、配置、密钥位置、排查）见沙箱备忘
  `~/.cache/opencode/tmp/tdai/NOTES.md`（本机专用，不进仓库）。
- **测试环境**：`~/.cache/opencode/tmp/da-fusion/`（配置指向代理；跑法见沙箱备忘）。
- **基线**：腾讯源码 `workspace/tdai-memory`，分支 `feat/server_team`，commit `0468a2a`。

### 关联

- 与 DC-16（长记忆封版）的关系：**新引擎是另一条路线**，最终二选一；本轮不动 DC-16 的既有成果。
- DC-19 活人感：记忆引擎只负责"记得住"，活人感靠人设 + 上下文组织，两者一起调。
