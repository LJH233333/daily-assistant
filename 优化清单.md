# daily-assistant 优化清单

> 项目: daily-assistant — OpenCode 日常陪伴助手（把编程 agent 改造成陪伴/管家/复盘助手，最终并入安卓 App）
> 交付物: `plugin/`（可直接装入 OpenCode） | 沙箱: `workspace/`
> 基线: OpenCode 1.18.29（Termux 移植版） | 测试: 隔离环境跑 `opencode run --agent <role> "<话>"`
> 插件源码: `plugin/plugin/daily-companion.ts` | 角色: `plugin/agent/*.md`

## 文档规范

1. **本文件只跟踪未完成项与在办项**，已闭合的迭代写一行结论即可
2. **迭代编号** — DC-N，完成即闭合，新迭代追加到末尾
3. **状态标记** — ⏳ 待定 | 🔄 进行中 | ✅ 完成 | ❌ 阻塞/暂缓
4. **隔离铁律** — 任何实验必须同时隔离 `XDG_CONFIG_HOME` + `XDG_DATA_HOME`，禁止污染主库（见 DC-7）

---

## 当前状态：DC-1~DC-7、DC-13、DC-14 ✅ 已完成（插件后端 + 公开仓库 + 规范）｜ DC-8~DC-12 ⏳ 未开始

- **版本**：插件 v0.1（后端）；未打包、未并入 App。
- **已验证能力**：记忆增/改/删、字数上限、去重、敏感扫描、长期待批准、历史检索（仅陪伴会话）、梦境复盘+日记，均实测通过。

---

## DC-1 ✅ 插件骨架 + 三角色（2026-09-13）

- 交付 `plugin/agent/` 下三个角色：`companion`（默认，陪伴）、`monitor`（手机管家）、`scheduler`（定时唤醒）。
- `plugin/opencode.jsonc`：默认角色、模型、技能路径、记忆注入。
- 验证：`opencode agent list` 三角色齐；persona 生效（读到 USER.md 的"讨厌啰嗦"）。

## DC-2 ✅ 记忆系统：增删改 + 上限 + 去重 + 敏感扫描（2026-09-13）

- `memory` 工具：`add/replace/remove`，`replace/remove` 用子串定位（多匹配报错）。
- 长期档案上限：USER 1375 字 / MEMORY 2200 字；**写满报错并回显当前条目，逼 AI 当场整理**。
- 写入前自动去重（保留首个）+ 敏感内容/注入扫描。
- 原子写（临时文件 + rename）防写坏。
- 验证：记住偏好 ✓ / 修改旧条目 ✓ / 灌满后新增触发报错→自行删旧腾位→写入 ✓ / 跨会话回忆 ✓。

## DC-3 ✅ 长期写入待批准队列（2026-09-13）

- 用户拍板：`target=daily` 立即写；`target=user/memory` 进 `pending.md` 待批准。
- `memory_review`：`list` / `approve`（all 或指定 id）/ `reject`。
- 验证：提案不入档且提示"待确认" ✓ / 批准后写入 USER.md 且队列清空 ✓。

## DC-4 ✅ 历史检索：只搜手机端陪伴会话（2026-09-13）

- `recall_history` 直读 OpenCode 会话库（`opencode.db`），按 `session.agent IN (companion/monitor/scheduler/dream)` 过滤。
- **范围铁律**：只搜手机端日常助手，绝不触碰电脑端与编程会话（用户要求）。
- 库路径可用 `DAILY_COMPANION_SESSION_DB` 覆盖。
- 验证：搜索"咖啡"命中历史原话 ✓。

## DC-5 ✅ 深夜复盘 dream 角色 + 梦境日记（2026-09-13）

- `dream` 子角色：读最近流水 → 提炼长期候选 → 提交待批准 → 写 `dreams.md`。
- `dream_diary` 工具写第一人称日记；日记只给人看，**不作为记忆来源**（对齐 OpenClaw）。
- 验证：一次复盘提 2 条候选 + 写日记 ✓。

## DC-6 ✅ 手机能力（2026-09-13）

- `phone_status` 工具：电量/充电/电池温度/CPU 温度/内存/运行时长。
- `phone-hands` 技能：截图、读屏文字、点击/滑动/输入、启动 App、通知/说话/震动（依赖 Termux:API 与 adb 无线调试）。
- 验证：读出电量/温度/内存/54 天未重启，并主动提醒温度偏高 ✓。

## DC-7 ✅ 测试隔离规范 + 污染事故清理（2026-09-13）

- **事故**：早期测试只隔离配置、未隔离数据目录 → 测试会话写进主库，暴露到用户 App。
- **修复**：确立"双隔离"（`XDG_CONFIG_HOME` + `XDG_DATA_HOME`）；并删除主库中我造的 13 个测试会话（22 → 9，剩余均用户自有）。
- **规范**：此后所有实验一律双隔离，禁止在主库产生会话。

---

## DC-8 ⏳ 并入安卓 App（未开始）

- 目标：把 `plugin/` 整套并入 `OConnector-Pro`，随 App 开关。
- 待定：内嵌方式（打包 Bun+opencode 引擎 / 嵌终端+完整环境）——两条路线已验证可行性，待用户拍板。
- 依赖：用户 App 源码（GitHub `LJH233333/OConnector-Pro`，当前仓库未含 `app/src`）。

## DC-9 ⏳ 夜间定时触发（未接）

- 现状：`dream` 需外部定时器触发；手机上可用 Termux cron，App 内用 App 定时器。
- 待办：手机端先配一个夜间定时（几点待定），跑 `opencode run --agent dream "开始今晚的复盘"`。

## DC-10 ⏳ 晋升评分门槛（OpenClaw 式，未做）

- 现状：`dream` 提案无评分门槛，靠模型判断，可能提案偏多。
- 计划：引入"评分 + 被回忆次数 + 查询多样性"三道门槛后再提案（OpenClaw 默认 0.75/3/3）。

## DC-11 ⏳ 向量检索记忆（评估后暂不采用）

- 调研过 `opencode-mem`（本地向量库，1642★，MIT）等，能力强但依赖嵌入模型 + AI 接口，体积与成本不适合手机端嵌入。
- 结论：先用"小档案 + 全文检索"，需要时再评估。

## DC-12 ⏳ 手机能力原生化（未开始）

- 现状：`phone_status` 依赖 Termux:API；截图/操作依赖 adb。
- 计划：App 内改为原生接口（UsageStatsManager / 无障碍 / 通知监听），并做成标准工具，见手机能力设计（电量/用量/屏幕/操作/通知）。

---

## DC-13 ✅ 记忆模板与实盘分离 + 公开仓库（2026-09-13）

- **问题**：记忆文件以后会被写入个人数据，若与模板同文件，公开仓库会泄露隐私。
- **做法**：仓库只保存 `USER.template.md` / `MEMORY.template.md`；实盘 `USER.md`/`MEMORY.md`、`daily/`、`pending.md`、`dreams.md` 及凭据、`workspace/`、`*.db` 由 `.gitignore` 排除。
- 新增 `install.sh`：装到 OpenCode；实盘记忆不存在时才用模板生成（不覆盖）；`opencode.jsonc` 已存在则备份不覆盖；支持 `OPENCODE_CONFIG_DIR` 指到隔离目录。
- 建**公开**仓库并推送：https://github.com/LJH233333/daily-assistant
- 验证：云端文件清单 18 个，无实盘记忆/凭据/沙箱；工作树 + git 全历史扫描零真实密钥。

## DC-14 ✅ 项目规范沉淀（2026-09-13）

- `AGENTS.md` 重写：五条铁律（新增"公开仓库红线"）、命令速查、**脱敏与凭证专节**、8 条坑+反例。
- 与全局规范去重（§0 对应表）；消解"全局允许另起服务 vs 本项目不碰运行实例"的潜在冲突（项目收紧）。
- 新增坑：实盘记忆入公开仓库、对全局配置跑 install.sh、本环境 task 子进程可能被拒。

---

## 关联记录

- 云端仓库（公开）：https://github.com/LJH233333/daily-assistant
- 记忆哲学调研：Hermes Agent（MIT，`NousResearch/hermes-agent`）与 OpenClaw（`openclaw/openclaw`）对比，结论 = 两者合体（小档案常驻 + 历史可搜 + 夜间门槛化晋升）。
- ⚠️ 安全提示：OpenClaw 仓库指标异常（星标/issue/体积离谱）、安装脚本不在仓库内，**仅作设计参考，禁止运行其安装脚本**。
- 已暂缓路线：`workspace/termux-app`（fork Termux 嵌终端）与 GitHub 仓库 `LJH233333/termux-assistant`（Android 内嵌实验，已停）。
